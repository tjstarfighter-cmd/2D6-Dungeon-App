# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Card images live in `public/cards/` and ARE committed so the GitHub
# Pages deploy can serve them without access to the gitignored docs/
# source folder. The sync script stays as a convenience for refreshing
# them locally when source PNGs change.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
